# Plan Fix — Issue 1648
**Hóa đơn hiển thị sai số lượng quy đổi khi đơn có CTKM PA**

## Thông tin
- Branch: `fixbug-issue-1648/tinhlm`
- Base: `release/1.0.0-avntt-rc1`
- Priority: High | Type: Issue
- Đơn mẫu: SO0000008170

## Root Cause (Đã xác nhận qua DB + code)

**Đơn SO0000008170 có:**
- HỘP QUÀ TẾT 2023 (140002773): Qty=1 CAS, IsPA=true, IsFreeItem=true, **CnvtFact=6**
- ProductComponents: mỗi component Quantity=1 (per 1 cái/box - base unit)
- Tức là 1 CAS = 6 hộp, mỗi hộp có N component

**Bug trong `BuildPAExpandedItemsAsync`** — file:
`modules/hqsoft.sap.dmsintegration/src/HQSOFT.SAP.DMSIntegration.Application/InvoiceMapping/Services/SalesOrderToInvoiceMapper.cs`

```csharp
// HIỆN TẠI (SAI):
var lineQty = (decimal)(line.Quantity ?? 0);        // = 1 (CAS)
var compQty = lineQty * (decimal)comp.Quantity;     // = 1 × 1 = 1 ← SAI

// ĐÚNG phải là:
var cnvtFact = (decimal)(line.CnvtFact > 0 ? line.CnvtFact : 1f);
var lineQty = (decimal)(line.Quantity ?? 0) * cnvtFact; // = 1 × 6 = 6
var compQty = lineQty * (decimal)comp.Quantity;          // = 6 × 1 = 6 ✓
```

`CnvtFact` tồn tại trong entity: `SalesOrderProduct.cs` line 192 → `public virtual float? CnvtFact { get; set; }`

## Phạm vi thay đổi
- **1 file:** `SalesOrderToInvoiceMapper.cs`
- **Vị trí:** trong method `BuildPAExpandedItemsAsync` — đầu vòng lặp `foreach (var (line, itemIndex, expandAll) in paLinesToExpand)`
- **Không ảnh hưởng** đến Case 1 (PA hàng bán không tặng) — cùng code path, cũng cần fix (đúng behavior)

## Fix Code

```csharp
// Tìm đoạn hiện tại:
var lineQty = (decimal)(line.Quantity ?? 0);
var componentsForPa = allComponents.Where(c => c.ProductId == line.ProductId).ToList();

// Sửa thành:
var cnvtFact = (decimal)(line.CnvtFact > 0 ? line.CnvtFact : 1f);
var lineQty = (decimal)(line.Quantity ?? 0) * cnvtFact;
var componentsForPa = allComponents.Where(c => c.ProductId == line.ProductId).ToList();
```

## Trace Comment
```csharp
// Issue 1648 | fixbug-issue-1648/tinhlm | <hash>
// Nhân CnvtFact vào lineQty để tính đúng số lượng quy đổi component PA trên hóa đơn.
```

## Lưu ý
- Sau fix: HỘP QUÀ TẾT Qty=1 CAS → mỗi component hiển thị 6 (thay vì 1)
- Các component bị duplicate trong DB (M-400G-L xuất hiện 2 lần trong ProductComponents): đây là data issue riêng, GroupBy đã xử lý bằng Sum → sẽ hiển thị 12 thay vì 6 nếu cả 2 row đều đúng. Cần xác nhận business với user.
- Không cần migration, không cần build (chỉ sửa .cs)
